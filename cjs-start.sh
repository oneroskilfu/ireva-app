#!/bin/bash

# Ultra-minimal startup script for Replit's 20-second timeout constraint
# Uses CommonJS format server which binds to port 5000 immediately

echo "Starting iREVA platform server (CommonJS version)..."
node server.cjs